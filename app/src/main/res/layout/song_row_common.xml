<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <include
        android:id="@+id/songTitleArtistArt"
        layout="@layout/song_title_artist_art"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_toStartOf="@+id/buttonContextMenu"

        />


    <ImageButton
        android:id="@+id/buttonContextMenu"
        android:layout_width="@dimen/fortyTwo"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@id/songTitleArtistArt"
        android:layout_alignParentEnd="true"
        android:layout_alignTop="@+id/songTitleArtistArt"
        android:layout_marginEnd="@dimen/eight"
        android:layout_marginStart="@dimen/sixteen"
        android:background="#0000"
        android:padding="@dimen/sixteen"
        android:scaleType="fitCenter"
        android:src="@drawable/context_menu_button"
        android:tint="#777" />

</RelativeLayout>